cd ~/nodejsmongo
npm install -g express
npm install -g express-generator
npm install -g grunt
npm install -g grunt-cli
npm install -g bower

#instalar ruby
#instalar ruby gems: ruby setup.rb
gem install compass

#instalar mongodb

npm install
